# rinkata-ticket-complete

Evidence-backed ticket completion for rinkata. It preflights the ticket, surfaces
ticket/spec context, then uses `rinkata_complete_ticket` so completion metadata and
protected evidence are written by rinkata instead of by hand.
